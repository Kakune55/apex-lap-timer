# Copy this file to .dev.vars for local `wrangler dev`
# No auth secrets are required for the current session-token login flow.
#
# User credentials are stored in D1 `users` table (password hash + salt),
# and login is handled by /api/auth/login.
